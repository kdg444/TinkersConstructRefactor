package slimeknights.tconstruct.datafix.schemas;

import com.mojang.datafixers.schemas.Schema;
import net.minecraft.util.datafix.schemas.NamespacedSchema;

/**
 * Class containing all block entities relevant to Hephaestus 1.20
 */
public class HephaestusSchema extends NamespacedSchema {

  public HephaestusSchema(int i, Schema schema) {
    super(i, schema);
  }
}
